---
// TechStack.astro
const techStack = {
  languages: [
    { name: "Python", icon: "🐍", color: "from-blue-500 to-yellow-500" },
    { name: "JavaScript", icon: "⚡", color: "from-yellow-400 to-yellow-600" },
    { name: "TypeScript", icon: "📘", color: "from-blue-600 to-blue-400" },
    { name: "SQL", icon: "🗄️", color: "from-orange-500 to-red-500" },
  ],
  frameworks: [
    { name: "Django", icon: "🎯", color: "from-green-600 to-green-800" },
    { name: "FastAPI", icon: "⚡", color: "from-teal-500 to-green-500" },
    { name: "React", icon: "⚛️", color: "from-blue-400 to-cyan-400" },
    { name: "Astro", icon: "🚀", color: "from-purple-500 to-pink-500" },
  ],
  tools: [
    { name: "PostgreSQL", icon: "🐘", color: "from-blue-600 to-blue-800" },
    { name: "Docker", icon: "🐳", color: "from-blue-500 to-blue-600" },
    { name: "AWS", icon: "☁️", color: "from-orange-400 to-yellow-500" },
    { name: "Git", icon: "📦", color: "from-red-500 to-orange-500" },
    { name: "Odoo ERP", icon: "💼", color: "from-purple-600 to-purple-800" },
  ],
};
---

<div class="bg-gray-900 rounded-2xl p-6 shadow-2xl border border-gray-800">
  <h2 class="text-2xl font-bold mb-6 text-center text-transparent bg-clip-text bg-gradient-to-r from-yellow-400 to-orange-400">
    🛠️ Tech Stack
  </h2>

  <div class="space-y-6">
    <!-- Languages -->
    <div>
      <h3 class="text-sm font-semibold text-gray-400 mb-3 uppercase tracking-wider">Lenguajes</h3>
      <div class="flex flex-wrap gap-2">
        {techStack.languages.map((tech) => (
          <div class={`group relative px-4 py-2 bg-gradient-to-r ${tech.color} rounded-lg text-white font-medium text-sm transition-all duration-300 hover:scale-105 hover:shadow-lg`}>
            <span class="mr-2">{tech.icon}</span>
            {tech.name}
          </div>
        ))}
      </div>
    </div>

    <!-- Frameworks -->
    <div>
      <h3 class="text-sm font-semibold text-gray-400 mb-3 uppercase tracking-wider">Frameworks</h3>
      <div class="flex flex-wrap gap-2">
        {techStack.frameworks.map((tech) => (
          <div class={`group relative px-4 py-2 bg-gradient-to-r ${tech.color} rounded-lg text-white font-medium text-sm transition-all duration-300 hover:scale-105 hover:shadow-lg`}>
            <span class="mr-2">{tech.icon}</span>
            {tech.name}
          </div>
        ))}
      </div>
    </div>

    <!-- Tools & Platforms -->
    <div>
      <h3 class="text-sm font-semibold text-gray-400 mb-3 uppercase tracking-wider">Herramientas & Plataformas</h3>
      <div class="flex flex-wrap gap-2">
        {techStack.tools.map((tech) => (
          <div class={`group relative px-4 py-2 bg-gradient-to-r ${tech.color} rounded-lg text-white font-medium text-sm transition-all duration-300 hover:scale-105 hover:shadow-lg`}>
            <span class="mr-2">{tech.icon}</span>
            {tech.name}
          </div>
        ))}
      </div>
    </div>
  </div>
</div>