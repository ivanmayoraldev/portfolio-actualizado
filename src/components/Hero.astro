---
const GITHUB_USERNAME = 'ivanmayoraldev';

interface GitHubUser {
  name: string;
  bio: string;
  location: string;
  blog: string;
  avatar_url: string;
  twitter_username: string;
  html_url: string;
}

let userData: GitHubUser | null = null;

try {
  const response = await fetch(`https://api.github.com/users/${GITHUB_USERNAME}`);
  if (response.ok) {
    userData = await response.json();
  }
} catch (error) {
  console.error('Error fetching GitHub data:', error);
}

const displayName = "Iván Mayoral Capel";
const displayBio = "Backend Developer Jr";
const displayLocation = "Madrid, España";
const avatarUrl = "./logo.jpg";

const education = [
    {
        title: "Desarrollo de Aplicaciones Web",
        institution: "CFP JuanXXIII",
        year: "2023 - 2025",
        description: "Frontend y Backend Development, Bases de Datos, Despliegue de Aplicaciones, Usabilidad y Accesibilidad.",
        icon: "🌐",
    },
    {
        title: "Desarrollo de Aplicaciones Multiplataforma",
        institution: "Sinergia FP",
        year: "2025 - Actualidad",
        description: "Acceso a Datos, Programación de Servicios y Procesos, Desarrollo de Interfaces, Sistemas de Gestión Empresarial, Desarrollo Móvil.",
        icon: "📱",
    }
];

const experience = [
    {
        position: "Desarrollador Backend Trainee",
        company: "Vortex Dimension Digital S.L",
        period: "Septiembre 2025 - Diciembre 2025",
        description: "Desarrollo de Módulos Custom en Odoo ERP utilizando Python, XML, PostgreSQL en entorno Ubuntu. Formación a empresas de mis proyectos desarrollados. Migración de bases de datos y módulos entre versiones de Odoo.",
        skills: ["Python", "XML", "PostgreSQL", "Ubuntu", "Odoo ERP"],
        icon: "💼",
    },
];

const techStack = [
    { name: "Python", icon: "🐍" },
    { name: "Django", icon: "🌐" },
    { name: "Odoo ERP", icon: "📦" },
    { name: "MySQL", icon: "🛢️" },
    { name: "PostgreSQL", icon: "🐘" },
    { name: "Docker", icon: "🐳" },
    { name: "Git", icon: "🔧" },
    { name: "AWS", icon: "☁️" },
    { name: "Linux", icon: "🐧" },
];

const certifications = [
    {name: "Cambridge English B2", icon: "🎓" },
];

const softSkills = [
    { name: "Trabajo en equipo", icon: "👥" },
    { name: "Resolución de problemas", icon: "🎯" },
    { name: "Aprendizaje continuo", icon: "📚" },
    { name: "Comunicación efectiva", icon: "💬" },
    { name: "Liderazgo técnico", icon: "🚀" },
];

const personalInfo = {
    interests: [
        { name: "Desarrollo de Software", icon: "💻" },
        { name: "Desarrollo Backend", icon: "🛠️" },
        { name: "Inteligencia Artificial", icon: "🤖" },
        { name: "Ciberseguridad", icon: "🔒" }
    ],
    languages: [
        { name: "Español", level: "Nativo"},
        { name: "Inglés", level: "B2" }
    ],
};
---

<div class="relative flex flex-col xl:flex-row items-center gap-8 xl:gap-12" id="home">

  <!-- Columna izquierda: Avatar y Badge -->
  <div class="relative flex-shrink-0 flex flex-col items-center gap-6 min-h-[160px] justify-center">
    <!-- Avatar más grande -->
    <div class="relative group">
      <!-- Anillo exterior -->
      <div class="absolute inset-0 flex items-center justify-center">
        <div class="w-36 h-36 rounded-full border border-yellow-500/30 animate-spin-slow"></div>
      </div>
      
      <!-- Glow effect -->
      <div class="absolute inset-0 bg-gradient-to-br from-yellow-400 via-orange-500 to-red-500 rounded-full blur-xl opacity-30 group-hover:opacity-50 transition-opacity duration-500 animate-pulse-slow"></div>
      
      <!-- Avatar -->
      <img
        class="relative rounded-full shadow-2xl size-32 ring-2 ring-white/10 transition-all duration-500 group-hover:scale-105 group-hover:ring-yellow-500/50 z-10"
        src={avatarUrl}
        alt={displayName}
        loading="eager"
      />
    </div>

    <!-- Badge disponible -->
    <div class="inline-flex items-center gap-2 px-4 py-2 bg-gradient-to-r from-green-500/90 to-emerald-500/90 text-white text-sm font-semibold rounded-full shadow-lg hover:shadow-green-500/50 hover:scale-105 transition-all duration-300">
      <span class="relative flex h-2 w-2">
        <span class="animate-ping absolute inline-flex h-full w-full rounded-full bg-white opacity-75"></span>
        <span class="relative inline-flex rounded-full h-2 w-2 bg-white"></span>
      </span>
      Disponible
    </div>
  </div>

  <!-- Columna derecha: Información y acciones -->
  <div class="relative flex-1 text-center lg:text-left space-y-3">
    <!-- Nombre compacto -->
    <div>
      <h1 class="text-3xl lg:text-4xl font-bold tracking-tight leading-tight mb-1">
        <span class="text-transparent bg-clip-text bg-gradient-to-r from-white via-gray-100 to-white animate-gradient">
          {displayName}
        </span>
      </h1>
      
      <!-- Título con separadores inline -->
      <div class="flex items-center justify-center lg:justify-start gap-2 flex-wrap">
        <span class="text-sm font-semibold text-transparent bg-clip-text bg-gradient-to-r from-yellow-400 to-orange-400">
          {displayBio.split('|')[0].trim()}
        </span>
        <span class="text-gray-600">•</span>
        <span class="text-xs text-gray-400 flex items-center gap-1">
          <svg class="w-3 h-3" fill="currentColor" viewBox="0 0 20 20">
            <path fill-rule="evenodd" d="M5.05 4.05a7 7 0 119.9 9.9L10 18.9l-4.95-4.95a7 7 0 010-9.9zM10 11a2 2 0 100-4 2 2 0 000 4z" clip-rule="evenodd"/>
          </svg>
          {displayLocation}
        </span>
      </div>
    </div>

    <!-- Descripción compacta -->
    <div class="max-w-2xl">
      <p class="text-sm text-gray-300 leading-relaxed">
        <span class="text-gray-200 font-medium">23 años.</span> Especializado en 
        <span class="text-yellow-400 font-medium">Python, Django, MySQL/PostgreSQL</span> y 
        <span class="text-orange-400 font-medium">diversas librerias como Pandas y PySpark</span>.
        <span class="text-gray-400">Graduado en DAW, cursando DAM y próximamente en 2026: Ingeniería Informática.</span>
        <span class="text-gray-400">Mejorando día a día como Backend Developer aplicando buenas prácticas y patrones de diseño en mis proyectos.</span>
      </p>
    </div>

    <!-- Botones compactos en fila -->
    <div class="flex flex-wrap items-center justify-center lg:justify-start gap-2">
      <!-- Botón CV - Principal -->
      <a
        href="/cv.pdf"
        download="Ivan_Mayoral_Capel_CV.pdf"
        class="group relative inline-flex items-center gap-1.5 px-4 py-2 bg-gradient-to-r from-purple-600 to-purple-700 hover:from-purple-700 hover:to-purple-800 text-white text-sm font-semibold rounded-lg transition-all duration-300 hover:scale-105 shadow-lg hover:shadow-purple-500/50"
      >
        <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 10v6m0 0l-3-3m3 3l3-3m2 8H7a2 2 0 01-2-2V5a2 2 0 012-2h5.586a1 1 0 01.707.293l5.414 5.414a1 1 0 01.293.707V19a2 2 0 01-2 2z"></path>
        </svg>
        CV
      </a>

      <!-- Botón LinkedIn -->
      <a
        href="https://linkedin.com/in/ivan-mayoral-capel"
        target="_blank"
        rel="noopener noreferrer"
        class="group inline-flex items-center gap-1.5 px-4 py-2 backdrop-blur-xl bg-white/5 border border-white/10 hover:bg-white/10 text-gray-300 hover:text-white text-sm font-semibold rounded-lg transition-all duration-300 hover:scale-105 hover:border-blue-500/50 hover:shadow-lg hover:shadow-blue-500/20"
      >
        <svg class="w-4 h-4 group-hover:text-blue-400" fill="currentColor" viewBox="0 0 24 24">
          <path d="M20.447 20.452h-3.554v-5.569c0-1.328-.027-3.037-1.852-3.037-1.853 0-2.136 1.445-2.136 2.939v5.667H9.351V9h3.414v1.561h.046c.477-.9 1.637-1.85 3.37-1.85 3.601 0 4.267 2.37 4.267 5.455v6.286zM5.337 7.433c-1.144 0-2.063-.926-2.063-2.065 0-1.138.92-2.063 2.063-2.063 1.14 0 2.064.925 2.064 2.063 0 1.139-.925 2.065-2.064 2.065zm1.782 13.019H3.555V9h3.564v11.452zM22.225 0H1.771C.792 0 0 .774 0 1.729v20.542C0 23.227.792 24 1.771 24h20.451C23.2 24 24 23.227 24 22.271V1.729C24 .774 23.2 0 22.222 0h.003z"/>
        </svg>
        LinkedIn
      </a>

      <!-- Botón GitHub -->
      <a
        href="https://github.com/ivanmayoraldev"
        target="_blank"
        rel="noopener noreferrer"
        class="group inline-flex items-center gap-1.5 px-4 py-2 backdrop-blur-xl bg-white/5 border border-white/10 hover:bg-white/10 text-gray-300 hover:text-white text-sm font-semibold rounded-lg transition-all duration-300 hover:scale-105 hover:border-gray-400/50 hover:shadow-lg hover:shadow-gray-500/20"
      >
        <svg class="w-4 h-4" fill="currentColor" viewBox="0 0 24 24">
          <path d="M12 0c-6.626 0-12 5.373-12 12 0 5.302 3.438 9.8 8.207 11.387.599.111.793-.261.793-.577v-2.234c-3.338.726-4.033-1.416-4.033-1.416-.546-1.387-1.333-1.756-1.333-1.756-1.089-.745.083-.729.083-.729 1.205.084 1.839 1.237 1.839 1.237 1.07 1.834 2.807 1.304 3.492.997.107-.775.418-1.305.762-1.604-2.665-.305-5.467-1.334-5.467-5.931 0-1.311.469-2.381 1.236-3.221-.124-.303-.535-1.524.117-3.176 0 0 1.008-.322 3.301 1.23.957-.266 1.983-.399 3.003-.404 1.02.005 2.047.138 3.006.404 2.291-1.552 3.297-1.23 3.297-1.23.653 1.653.242 2.874.118 3.176.77.84 1.235 1.911 1.235 3.221 0 4.609-2.807 5.624-5.479 5.921.43.372.823 1.102.823 2.222v3.293c0 .319.192.694.801.576 4.765-1.589 8.199-6.086 8.199-11.386 0-6.627-5.373-12-12-12z"/>
        </svg>
        GitHub
      </a>

      <!-- Botón Email -->
      <a
        href="mailto:ivanmayoral.dev@gmail.com"
        class="group inline-flex items-center gap-1.5 px-4 py-2 backdrop-blur-xl bg-white/5 border border-white/10 hover:bg-white/10 text-gray-300 hover:text-white text-sm font-semibold rounded-lg transition-all duration-300 hover:scale-105 hover:border-yellow-500/50 hover:shadow-lg hover:shadow-yellow-500/20"
      >
        <svg class="w-4 h-4 group-hover:text-yellow-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 8l7.89 5.26a2 2 0 002.22 0L21 8M5 19h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v10a2 2 0 002 2z"/>
        </svg>
        Email
      </a>
    </div>
  </div>

  <!-- Columna derecha: Slider de Información -->
  <div class="relative flex-shrink-0 hidden xl:block">
    <div class="backdrop-blur-xl bg-white/5 border border-white/10 rounded-xl p-4 w-80 overflow-hidden">
      <!-- Slider Container con altura fija -->
      <div id="info-slider" class="relative h-64">
        
        <!-- Slide 1: Estudios -->
        <div class="slide active absolute inset-0" data-slide="education">
          <h3 class="text-sm font-bold mb-3 flex items-center gap-2 text-transparent bg-clip-text bg-gradient-to-r from-blue-400 to-cyan-400">
            <svg class="w-4 h-4 text-blue-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 14l9-5-9-5-9 5 9 5zM12 14l6.16-3.422a12.083 12.083 0 01.665 6.479A11.952 11.952 0 0012 20.055a11.952 11.952 0 00-6.824-2.998 12.078 12.078 0 01.665-6.479L12 14zm-4 6v-7.5l4-2.222"/>
            </svg>
            Estudios
          </h3>
          <div class="space-y-2">
            {education.map((edu) => (
              <div class="flex items-start gap-2 bg-white/5 rounded-lg p-2 hover:bg-white/10 transition-all duration-300 border border-white/5 hover:border-white/10">
                <div class="text-sm flex-shrink-0">{edu.icon}</div>
                <div class="min-w-0 flex-1">
                  <div class="text-xs text-white font-medium line-clamp-1">{edu.title}</div>
                  <div class="text-[10px] text-cyan-400 line-clamp-1">{edu.institution}</div>
                  <div class="text-[9px] text-gray-500 mt-0.5">{edu.year}</div>
                </div>
              </div>
            ))}
            {education.length < 4 && (
              <div class="flex items-center gap-2 bg-white/5 rounded-lg p-2 opacity-50">
                <div class="text-sm">🎓</div>
                <div class="min-w-0 flex-1">
                  <div class="text-xs text-white font-medium truncate">Próximos estudios</div>
                  <div class="text-[10px] text-cyan-400 truncate">2026 - Ingeniería Informática</div>
                </div>
              </div>
            )}
          </div>
        </div>

        <!-- Slide 2: Experiencia -->
        <div class="slide absolute inset-0" data-slide="experience">
          <h3 class="text-sm font-bold mb-3 flex items-center gap-2 text-transparent bg-clip-text bg-gradient-to-r from-purple-400 to-pink-400">
            <svg class="w-4 h-4 text-purple-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M21 13.255A23.931 23.931 0 0112 15c-3.183 0-6.22-.62-9-1.745M16 6V4a2 2 0 00-2-2h-4a2 2 0 00-2 2v2m4 6h.01M5 20h14a2 2 0 002-2V8a2 2 0 00-2-2H5a2 2 0 00-2 2v10a2 2 0 002 2z"/>
            </svg>
            Experiencia
          </h3>
          <div class="space-y-2">
            {experience.map((job) => (
              <div class="bg-white/5 rounded-lg p-3 hover:bg-white/10 transition-all duration-300 border border-white/5 hover:border-white/10">
                <div class="flex items-start gap-2 mb-2">
                  <div class="text-lg">{job.icon}</div>
                  <div class="min-w-0 flex-1">
                    <div class="text-xs text-white font-bold line-clamp-1">{job.position}</div>
                    <div class="text-[10px] text-purple-400 line-clamp-1">{job.company}</div>
                    <div class="text-[9px] text-gray-500">{job.period}</div>
                  </div>
                </div>
                <div class="flex flex-wrap gap-1">
                  {job.skills.slice(0, 3).map((skill) => (
                    <span class="px-1.5 py-0.5 text-[9px] rounded bg-purple-500/20 text-purple-300 font-medium border border-purple-500/30">
                      {skill}
                    </span>
                  ))}
                  {job.skills.length > 3 && (
                    <span class="px-1.5 py-0.5 text-[9px] rounded bg-gray-500/20 text-gray-400 font-medium">
                      +{job.skills.length - 3}
                    </span>
                  )}
                </div>
              </div>
            ))}
          </div>
        </div>

        <!-- Slide 3: Tech Stack -->
        <div class="slide absolute inset-0" data-slide="stack">
          <h3 class="text-sm font-bold mb-3 flex items-center gap-2 text-transparent bg-clip-text bg-gradient-to-r from-yellow-400 to-orange-400">
            <svg class="w-4 h-4 text-yellow-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10 20l4-16m4 4l4 4-4 4M6 16l-4-4 4-4"/>
            </svg>
            Stack Técnico
          </h3>
          <div class="grid grid-cols-2 gap-2">
            {techStack.map((tech) => (
              <div class="flex items-center gap-2 bg-white/5 rounded-lg p-2 hover:bg-white/10 transition-all duration-300 border border-white/5 hover:border-white/10">
                <div class="text-base">{tech.icon}</div>
                <div class="min-w-0 flex-1">
                  <div class="text-xs text-white font-medium truncate">{tech.name}</div>
                </div>
              </div>
            ))}
          </div>
        </div>

        <!-- Slide 4: Idiomas -->
        <div class="slide absolute inset-0" data-slide="languages">
          <h3 class="text-sm font-bold mb-3 flex items-center gap-2 text-transparent bg-clip-text bg-gradient-to-r from-green-400 to-teal-400">
            <svg class="w-4 h-4 text-green-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M8 16H6a2 2 0 01-2-2V6a2 2 0 012-2h8a2 2 0 012 2v2m-6 12h6a2 2 0 002-2v-8a2 2 0 00-2-2h-8a2 2 0 00-2 2v6"/>
            </svg>
            Idiomas
          </h3>
          <div class="space-y-3">
            {personalInfo.languages.map((lang) => (
              <div>
                <div class="flex items-center justify-between mb-1">
                  <span class="text-xs text-white font-medium">{lang.name}</span>
                  <span class="text-[10px] text-green-400">{lang.level}</span>
                </div>
              </div>
            ))}
          </div>
          <div class="mt-4 pt-3 border-t border-white/10">
            <h4 class="text-xs font-semibold text-gray-300 mb-2">Intereses</h4>
            <div class="flex flex-wrap gap-1.5">
              {personalInfo.interests.map((interest) => (
                <span class="flex items-center gap-1 px-2 py-1 text-[9px] rounded-lg bg-green-500/20 text-green-300 font-medium border border-green-500/30">
                  <span>{interest.icon}</span>
                  <span class="hidden sm:inline">{interest.name}</span>
                </span>
              ))}
            </div>
          </div>
        </div>

        <!-- Slide 5: Certificaciones -->
        <div class="slide absolute inset-0" data-slide="certifications">
          <h3 class="text-sm font-bold mb-3 flex items-center gap-2 text-transparent bg-clip-text bg-gradient-to-r from-cyan-400 to-blue-400">
            <svg class="w-4 h-4 text-cyan-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12h6m-6 4h6m2 5H7a2 2 0 01-2-2V5a2 2 0 012-2h5l2 2h5a2 2 0 012 2v12a2 2 0 01-2 2z"/>
            </svg>
            Certificaciones
          </h3>
          <div class="space-y-2">
            {certifications.map((cert) => (
              <div class="flex items-center gap-3 px-3 py-2.5 bg-cyan-500/20 rounded-lg border border-cyan-500/30 hover:bg-cyan-500/30 transition-all duration-300">
                <div class="text-xl">{cert.icon}</div>
                <span class="text-xs text-cyan-300 font-medium">{cert.name}</span>
              </div>
            ))}
          </div>
          <div class="mt-4 pt-3 border-t border-white/10">
            <h4 class="text-xs font-semibold text-gray-300 mb-2">Soft Skills</h4>
            <div class="flex flex-wrap gap-1.5">
              {softSkills.slice(0, 4).map((skill) => (
                <span class="flex items-center gap-1 px-2 py-1 text-[9px] rounded-lg bg-cyan-500/20 text-cyan-300 font-medium border border-cyan-500/30">
                  <span>{skill.icon}</span>
                  <span>{skill.name}</span>
                </span>
              ))}
            </div>
          </div>
        </div>

      </div>

      <!-- Indicadores del Slider -->
      <div class="flex justify-center gap-1.5 mt-3">
        <div class="indicator active" data-slide="education"></div>
        <div class="indicator" data-slide="experience"></div>
        <div class="indicator" data-slide="stack"></div>
        <div class="indicator" data-slide="languages"></div>
        <div class="indicator" data-slide="certifications"></div>
      </div>
    </div>
  </div>
</div>

<style>
  @keyframes float {
    0%, 100% { transform: translate(0, 0) scale(1); }
    33% { transform: translate(30px, -30px) scale(1.1); }
    66% { transform: translate(-20px, 20px) scale(0.9); }
  }

  @keyframes float-delayed {
    0%, 100% { transform: translate(0, 0) scale(1); }
    33% { transform: translate(-30px, 30px) scale(0.9); }
    66% { transform: translate(20px, -20px) scale(1.1); }
  }

  @keyframes spin-slow {
    from { transform: rotate(0deg); }
    to { transform: rotate(360deg); }
  }

  @keyframes gradient {
    0%, 100% { background-position: 0% 50%; }
    50% { background-position: 100% 50%; }
  }

  @keyframes pulse-slow {
    0%, 100% { opacity: 0.3; }
    50% { opacity: 0.5; }
  }

  .animate-float {
    animation: float 20s ease-in-out infinite;
  }

  .animate-float-delayed {
    animation: float-delayed 25s ease-in-out infinite;
  }

  .animate-spin-slow {
    animation: spin-slow 20s linear infinite;
  }

  .animate-gradient {
    background-size: 200% 200%;
    animation: gradient 3s ease infinite;
  }

  .animate-pulse-slow {
    animation: pulse-slow 4s ease-in-out infinite;
  }

  /* Slider Styles */
  .slide {
    opacity: 0;
    pointer-events: none;
    transition: opacity 0.5s ease-in-out;
  }

  .slide.active {
    opacity: 1;
    pointer-events: auto;
  }

  .indicator {
    width: 6px;
    height: 6px;
    border-radius: 50%;
    background-color: rgba(255, 255, 255, 0.3);
    cursor: pointer;
    transition: all 0.3s ease;
  }

  .indicator.active {
    background-color: rgba(59, 130, 246, 0.8);
    width: 16px;
    border-radius: 3px;
  }

  .indicator:hover {
    background-color: rgba(255, 255, 255, 0.5);
    transform: scale(1.2);
  }

  .line-clamp-1 {
    display: -webkit-box;
    -webkit-line-clamp: 1;
    -webkit-box-orient: vertical;
    overflow: hidden;
  }
</style>

<script>
  document.addEventListener('DOMContentLoaded', function() {
    const slides = document.querySelectorAll('.slide');
    const indicators = document.querySelectorAll('.indicator');
    let currentSlide = 0;
    const totalSlides = slides.length;
    let autoRotateInterval: number | undefined;

    function showSlide(index: number) {
      // Ocultar todos los slides
      slides.forEach(slide => slide.classList.remove('active'));
      indicators.forEach(indicator => indicator.classList.remove('active'));

      // Mostrar el slide actual
      slides[index].classList.add('active');
      indicators[index].classList.add('active');

      currentSlide = index;
    }

    function nextSlide() {
      const nextIndex = (currentSlide + 1) % totalSlides;
      showSlide(nextIndex);
    }

    function startAutoRotate() {
      autoRotateInterval = setInterval(nextSlide, 8000);
    }

    function stopAutoRotate() {
      if (autoRotateInterval) clearInterval(autoRotateInterval);
    }

    // Iniciar auto-rotación
    startAutoRotate();

    // Click en indicadores para cambiar manualmente
    indicators.forEach((indicator, index) => {
      indicator.addEventListener('click', () => {
        stopAutoRotate();
        showSlide(index);
        startAutoRotate(); // Reiniciar después de interacción manual
      });
    });

    // Pausar auto-rotación al hacer hover
    const sliderContainer = document.querySelector('#info-slider')?.parentElement;
    if (sliderContainer) {
      sliderContainer.addEventListener('mouseenter', stopAutoRotate);
      sliderContainer.addEventListener('mouseleave', startAutoRotate);
    }
  });
</script>